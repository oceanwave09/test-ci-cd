CREATE TABLE IF NOT EXISTS cynchealth_staging.gap_care (
    row_id VARCHAR,
    row VARCHAR,
    domain VARCHAR,
    patient_first_name VARCHAR,
    patient_last_name VARCHAR,
    patient_dob VARCHAR,
    external_patient_id VARCHAR,
    external_practice_id VARCHAR,
    code VARCHAR,
    name VARCHAR,
    dictionary_name VARCHAR,
    date_performed VARCHAR,
    date_ordered VARCHAR,
    date_due VARCHAR,
    refused VARCHAR,
    med_unnecessary_reason_code VARCHAR,
    med_unnecessary_reason_name VARCHAR,
    med_unnecessary_reason_dictionary VARCHAR,
    visit_reason_code VARCHAR,
    visit_reason_name VARCHAR,
    visit_reason_dictionary VARCHAR,
    provider_npi VARCHAR,
    practice_tin VARCHAR,
    provider_name VARCHAR,
    remove_care_item VARCHAR,
    value VARCHAR,
    modifier_dictionary_name VARCHAR,
    modifier_code VARCHAR,
    vendor VARCHAR,
    source_type VARCHAR,
    patient_id VARCHAR,
    file_batch_id VARCHAR,
    file_name VARCHAR,
    file_source_name VARCHAR,
    file_status VARCHAR,
    created_user VARCHAR,
    created_ts TIMESTAMP(3) WITH TIME ZONE,
    updated_user VARCHAR,
    updated_ts TIMESTAMP(3) WITH TIME ZONE
)
WITH (
    LOCATION = 's3a://phm-development-datapipeline-bucket/delta-tables/cynchealth/staging/gap_care',
    PARTITIONED_BY = ARRAY['file_batch_id'],
    CHECKPOINT_INTERVAL = 5
);